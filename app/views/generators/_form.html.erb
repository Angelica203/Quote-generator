<%= form_with(model: generator) do |form| %>
  <% if generator.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(generator.errors.count, "error") %> prohibited this generator from being saved:</h2>

      <ul>
        <% generator.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quotes, style: "display: block" %>
    <%= form.text_field :quotes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
